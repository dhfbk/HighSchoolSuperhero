using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[ExecuteInEditMode]
public class GridWalls : MonoBehaviour
{
    //// Start is called before the first frame update
    //void Start()
    //{
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    transform.position = new Vector3(Mathf.Round(transform.position.x * 10f) / 10f, Mathf.Round(transform.position.y * 10f) / 10f, Mathf.Round(transform.position.z * 10f) / 10f);
    //    transform.localScale = new Vector3(Mathf.Round(transform.localScale.x * 5f) / 5f, Mathf.Round(transform.localScale.y * 5f) / 5f, Mathf.Round(transform.localScale.z * 5f) / 5f);
    //}
}
